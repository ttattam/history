# 🧠 Claude Code History - Personal Memory System

Автоматическая система управления историей всех разговоров с Claude Code с семантическим поиском, умным связыванием и граф-визуализацией.

## 🎯 Возможности

### 🔍 **Интеллектуальный поиск**
- **Семантический поиск**: "найди где мы обсуждали React hooks"
- **Поиск по файлам**: найти все разговоры упоминающие конкретные файлы
- **Фильтры**: по проектам, датам, типам сообщений
- **Hybrid поиск**: комбинация keyword + semantic similarity

### 🧠 **Умные связи**
- **Автоматическое связывание**: система находит похожие разговоры
- **Semantic clustering**: группировка по темам и контексту
- **Ручное связывание**: можешь вручную связать связанные беседы
- **Strength scoring**: оценка силы связи между разговорами

### 🕸️ **Граф визуализация**
- **Interactive graph**: интерактивная сеть всех разговоров
- **Force-directed layout**: физическая симуляция связей
- **Color coding**: цвета по темам (React, Database, DevOps, etc.)
- **Zoom & pan**: масштабирование и навигация
- **Node details**: полная информация при клике

### ⚡ **Автоматизация**
- **Real-time import**: автоматический импорт новых разговоров
- **Zero configuration**: работает из коробки
- **File watching**: отслеживает изменения в Claude Code history
- **Background processing**: не мешает работе

## 🚀 Быстрый старт

### 1. Запуск системы
```bash
cd /Users/tomtam/Developer/tools/history
PORT=3002 node api/server.js
```

### 2. Открыть веб интерфейс
```bash
open http://localhost:3002
```

### 3. Готово!
- 🔍 **Поиск**: http://localhost:3002
- 🕸️ **Граф**: http://localhost:3002/graph.html  
- 📊 **API**: http://localhost:3002/stats
- 📄 **Экспорт**: кнопка "📄 MD" рядом с каждым разговором

## 📋 После перезагрузки компьютера

Система не запускается автоматически. Чтобы снова использовать экспорт:

### Простой способ:
```bash
cd /Users/tomtam/Developer/tools/history
PORT=3002 node api/server.js
```
Затем открыть: http://localhost:3002

### В фоновом режиме:
```bash
cd /Users/tomtam/Developer/tools/history
PORT=3002 nohup node api/server.js > logs/server.log 2>&1 &
echo "Сервер запущен в фоне. Логи: logs/server.log"
```

### Проверить что работает:
```bash
curl http://localhost:3002/health
```
Должно вернуть: `{"status":"ok","timestamp":"..."}`

### Остановить сервер:
```bash
pkill -f "node.*server.js"
```

## 📋 Что происходит автоматически

1. **Импорт истории**: Система сразу импортирует всю историю из `~/.claude/history.jsonl`
2. **Генерация embeddings**: Создает векторные представления для семантического поиска  
3. **Автосвязывание**: Находит похожие разговоры и создает связи
4. **Мониторинг**: Отслеживает новые разговоры и импортирует их автоматически

## 🎛️ Управление

### Запуск
```bash
./start-system.sh
```

### Остановка  
```bash
./stop-system.sh
```

### Логи
```bash
# Auto-import логи
tail -f logs/auto-import.log

# API server логи  
tail -f logs/api-server.log
```

### Ручные операции
```bash
# Одноразовый импорт конкретного файла
node scripts/import-claude.js path/to/conversation.json

# Запуск автосвязывания для всех разговоров
curl -X POST http://localhost:3001/auto-link?threshold=0.7

# Проверка здоровья системы
curl http://localhost:3001/health
```

## 🏗️ Архитектура

### База данных
- **SQLite** - основные данные (conversations, messages, links)
- **FTS5** - полнотекстовый поиск 
- **Embeddings table** - векторные представления для семантики

### Backend
- **Node.js + FastAPI** - REST API сервер
- **Auto-import service** - мониторинг и импорт новых разговоров
- **Embedding service** - генерация semantic vectors
- **Linking service** - автоматическое связывание разговоров

### Frontend  
- **Vanilla JavaScript** - легкий и быстрый UI
- **D3.js** - граф визуализация
- **Responsive design** - работает на всех устройствах

## 📊 API Endpoints

### Основные
- `GET /` - главная страница
- `GET /graph.html` - граф визуализация
- `GET /health` - проверка здоровья системы
- `GET /stats` - статистика базы данных

### Поиск
- `GET /search?q=query&type=messages&limit=20` - поиск по сообщениям
- `GET /search?q=query&type=conversations` - поиск по разговорам

### Разговоры
- `GET /conversations?limit=20&project=path` - список разговоров
- `GET /conversations/:id` - конкретный разговор
- `GET /conversations/:id/links` - связи разговора
- `GET /conversations/:id/similar?threshold=0.6` - похожие разговоры

### Умные фичи
- `POST /auto-link?threshold=0.7` - запуск автосвязывания
- `POST /conversations/link` - ручное связывание разговоров

## 💾 Структура данных

### Conversations
```sql
conversations (
  id, title, started_at, updated_at, 
  message_count, project_path, tags, summary
)
```

### Messages  
```sql
messages (
  id, conversation_id, role, content, timestamp,
  tool_calls, file_references
)
```

### Links
```sql
conversation_links (
  from_id, to_id, link_type, strength, reason
)
```

### Embeddings
```sql
conversation_embeddings (
  conversation_id, embedding_vector, embedding_model
)
```

## 🔧 Конфигурация

### Environment Variables (.env)
```bash
PORT=3002                    # API server port  
DB_PATH=./history.db         # Database location
EMBEDDING_MODEL=local        # local | openai
OPENAI_API_KEY=your_key      # For OpenAI embeddings

## 🚨 Устранение проблем

### "Порт 3002 уже занят"
```bash
# Проверить что использует порт
lsof -i :3002

# Убить процесс если нужно
pkill -f "node.*server.js"

# Использовать другой порт
PORT=3003 node api/server.js
```

### "База данных пустая / нет разговоров"
```bash
# Импортировать историю заново
node scripts/universal-claude-importer.js

# Проверить что импортировалось
curl http://localhost:3002/stats
```

### "Не могу скачать markdown файл"
- Проверь что API работает: `curl http://localhost:3002/health`
- Попробуй другой браузер
- Проверь консоль браузера на ошибки

### "Не находит файл history.jsonl"
История Claude Code находится в `~/.claude/history.jsonl`. Если файла нет:
- Убедись что Claude Code установлен
- Проверь: `ls -la ~/.claude/`
- Может быть история в `~/.claude/projects/` (новый формат)

## 📁 Структура проекта

```
history/
├── api/                     # Node.js API сервер
│   ├── server.js           # Основной сервер (ЗАПУСКАТЬ ЭТО)
│   └── services/           # Эмбеддинги и связи
├── web/                    # Веб интерфейс
│   ├── index.html          # Главная страница
│   ├── graph.html          # Граф визуализация  
│   └── assets/             # JS и CSS
├── scripts/                # Импорт и утилиты
│   └── universal-claude-importer.js
├── database/               # Схема базы данных
└── README.md              # Эта инструкция
```

### Настройка поиска
- **Similarity threshold**: 0.7 (70% схожесть для автосвязывания)
- **Search limit**: 20 результатов по умолчанию
- **Embedding dimensions**: 384 (local model) или 1536 (OpenAI)

## 🎨 Использование

### Типичные запросы
- *"React optimization"* → найдет все разговоры про оптимизацию React
- *"database setup"* → покажет настройки баз данных  
- *"bug fix"* → все обсуждения багфиксов
- *"UserList.tsx"* → разговоры упоминающие конкретный файл

### Граф исследование
1. Открой `/graph.html`
2. Кликай на узлы для деталей
3. Используй threshold slider для фильтрации связей
4. Зумируй и перетаскивай для навигации

### Связывание разговоров
- **Автоматическое**: система сама находит похожие (>70% similarity)
- **Ручное**: можешь связать любые разговоры через UI или API

## 📈 Статистика

После импорта получишь:
- **Общее количество разговоров** из всей истории Claude Code
- **Сообщения** с полным текстом и метаданными  
- **Автоматические связи** между похожими разговорами
- **Проекты** с группировкой по папкам
- **Временной диапазон** всей истории общения

## 🚀 Производительность

### Оптимизации
- **SQLite indexes** для быстрых запросов
- **Vector similarity** через cosine distance
- **Batch processing** для embedding generation
- **Connection pooling** для database access

### Масштабируемость  
- ✅ **1,000 разговоров** - отличная производительность
- ✅ **10,000 разговоров** - хорошая производительность  
- ✅ **100,000+ разговоров** - потребуется PostgreSQL

## 🎯 Результат

**Ты получаешь superhuman память для всех разговоров с Claude Code!**

- 🔍 **Мгновенный поиск** любого обсуждения по смыслу
- 🧠 **Автоматические связи** между похожими темами  
- 🕸️ **Визуальная навигация** по всем разговорам
- ⚡ **Нулевая настройка** - работает автоматически
- 📊 **Полная статистика** твоего взаимодействия с AI

## 💡 Продвинутые фичи

### Кастомизация
- Измени цвета категорий в `web/assets/graph.js`  
- Настрой similarity threshold для более/менее жестких связей
- Добавь свои tech keywords в embedding service

### Интеграция
- API готов для интеграции с другими инструментами
- Export функции для резервного копирования
- Webhook support для внешних уведомлений

### Расширения  
- Добавление файловых вложений к разговорам
- Integration с Git commits для связи кода и обсуждений
- Mobile app через API

---

**🎉 Наслаждайся своей новой AI памятью!**

Теперь ни один разговор, идея или решение не потеряется. Вся твоя история общения с Claude Code организована, проиндексирована и доступна через интуитивный поиск и визуализацию.

**🚀 Запускай систему и исследуй свою историю общения с AI!**